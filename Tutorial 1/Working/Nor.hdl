/**
 * Nor gate:
 * if a or b out = 0, else out = 1
 */
CHIP Nor {
    IN a, b;
    OUT out;

    PARTS:
    Nand(a= a, b= a, out= notA);
    Nand(a= b, b= b, out= notB);
    Nand(a= notA, b= notB, out= notnotAnotB);
    Not(in= notnotAnotB, out= out);
}